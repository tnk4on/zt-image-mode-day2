<aside class="page-nav" role="navigation">
    <div class="page-nav-inner">
        {{#with page.previous}}
        {{#if url}}
        <a class="page-nav-btn prev" href="{{{relativize ../page.url url}}}">
            <span class="page-nav-label">前へ</span>
            <span class="page-nav-title">{{content}}</span>
        </a>
        {{/if}}
        {{/with}}
        {{#with page.next}}
        {{#if url}}
        <a class="page-nav-btn next" href="{{{relativize ../page.url url}}}">
            <span class="page-nav-label">次へ</span>
            <span class="page-nav-title">{{content}}</span>
        </a>
        {{/if}}
        {{/with}}
    </div>
</aside>