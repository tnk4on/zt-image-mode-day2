<nav class="nav" aria-label="Navigation">
    {{#each site.components}}
    <ul class="nav-list">
        {{#each versions}}
        {{#each navigation}}
        {{#each items}}
        {{#if url}}
        <li class="nav-item{{#if (eq url ../../../../page.url)}} is-current-page{{/if}}">
            <a class="nav-link" href="{{{relativize ../../../../page.url url}}}">{{content}}</a>
        </li>
        {{/if}}
        {{/each}}
        {{/each}}
        {{/each}}
    </ul>
    {{/each}}
</nav>